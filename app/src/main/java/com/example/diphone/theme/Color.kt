package com.example.diphone.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    val Background = Color(0xFF000000)        // Pure black
    val Surface = Color(0xFF1A1A1A)           // Dark card surface
    val SurfaceVariant = Color(0xFF2A2A2A)    // Slightly lighter
    val Primary = Color(0xFF00C853)           // Green (buttons, active)
    val PrimaryVariant = Color(0xFF69F0AE)    // Light green accent
    val Decline = Color(0xFFE53935)           // Red (decline, delete)
    val Warning = Color(0xFFFF6D00)           // Orange (missed, harassment)
    val OnBackground = Color(0xFFFFFFFF)      // White text
    val OnSurface = Color(0xFFE0E0E0)         // Light grey text
    val Subtitle = Color(0xFF9E9E9E)          // Grey subtitle
    val Divider = Color(0xFF2D2D2D)           // Dark divider
    val CallBackground = Color(0xFF3B1A1A)    // Dark brownish-red (call screens)
    val IncomingCallGradientTop = Color(0xFF4A1C1C)
    val IncomingCallGradientBottom = Color(0xFF000000)
    val Blocked = Color(0xFFFF6D00)           // Orange for blocked entries
    val HarassmentTag = Color(0xFFFF6D00)     // Same orange for harassment
    val VipStar = Color(0xFFFFC107)           // Gold for favourite star
    val WhatsAppGreen = Color(0xFF25D366)
}
